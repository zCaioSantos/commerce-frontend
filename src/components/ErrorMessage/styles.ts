import styled from 'styled-components';

export const Message = styled.span`
 font-family: 'Lato';
 font-size: 0.875rem;
 font-weight: 400;
 font-style: normal;
 width: 100%;
 position: 'relative';
 padding: '0.25rem 0';
 color: #f85757;
 bottom: 1rem;
 margin-top: -2px;
`;
