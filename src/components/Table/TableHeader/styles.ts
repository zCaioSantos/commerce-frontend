import styled from 'styled-components';

export const Header = styled.thead`
 th {
  padding: 20px 32px;
  text-align: left;
  font-weight: 700;
  color: #464646;

  &.small {
   width: 5px;
  }
 }
`;
